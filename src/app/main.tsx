import React from 'react';
import ReactDOM from 'react-dom/client';
import WeaklyPrices from './weaklyPrices/weaklyPrices';


ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WeaklyPrices />
  </React.StrictMode>,
)
